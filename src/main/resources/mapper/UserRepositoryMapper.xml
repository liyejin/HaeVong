<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.heabong.web.repository.UserRepository">
   <select id="findAll">
      select * from user
   </select>
   <select id="findByUserName">
	   select * from user where id = ${id}
   </select>
   <select id="findByUserPhoto">
	   SELECT profile_photo FROM user WHERE id = ${id}
   </select>
   <select id="findByUid" resultType="User">
	   select * from user where uid=#{uid};
   </select>
   <update id="update">
      UPDATE haevongdb.user
      <set>
        <if test="name != null">name = #{name},</if>
        <if test="phoneNumber != null">phone_number = #{phoneNumber},</if>
        <if test="gender != null">gender = #{gender},</if>
        <if test="age != null">age = #{age},</if>
        <if test="birthDate != null">birth_date = #{birthDate},</if>
        <if test="nickname != null">nickname = #{nickname}</if>
      </set>
   </update>
   <delete id="delete">
 delete from user where name = #{name}
   </delete>
   <insert id="save">
   INSERT INTO user(uid, name, email, phone_number,gender, age, birth_date, profile_photo, nickname)
		VALUES(uid=#{uid},name=#{name}, email=#{email}, phone_number=#{phoneNumber}, gender=#{gender}, age=#{age}, birth_date=#{birthDate}, profile_photo=#{profilePhoto}, nickname=#{nickname} );
   </insert>
   

   <!-- My page -->
    <select id="findUserInfoById">
      select * from user where id = ${id} ;
   </select>

   </mapper>
